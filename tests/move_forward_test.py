import math
def move_forward_test(auto_node):
    
        
    auto_node.move_forward(0.5)
    auto_node.rotatebot(90)
    auto_node.move_forward(0.5)
    auto_node.rotatebot(90)
    auto_node.move_forward(0.5)
    auto_node.rotatebot(90)
    auto_node.move_forward(0.5)
   
       
    